{
  "name": "cat-survivors-web",
  "version": "1.0.0",
  "description": "Cat Survivors — clone di Vampire Survivors con gatti, versione browser",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "pack": "npm run build && node scripts/pack.mjs",
    "relay": "node relay/server.js"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1",
    "jszip": "^3.10.1",
    "playwright": "^1.60.0",
    "typescript": "^5.6.3",
    "vite": "^6.0.7",
    "ws": "^8.21.0"
  }
}
